import React from "react";

const HorizonLine = ({ }) => {
  return (
    <div
      style={{
        width:"100%",
        borderBottom: "1px solid #aaa",
        lineHeight: "0.1em",
        margin: "30px 0 20px",
      }}
    >
    </div>
  );
};

export default HorizonLine;
